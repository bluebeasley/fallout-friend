<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <!--Splash screen theme-->
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name= "ButtonStyle">
        <item name="android:background">@layout/button</item>
        <item name="android:textSize">@dimen/text_size_med</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:paddingStart">@dimen/element_margin_large</item>
        <item name="android:paddingEnd">@dimen/element_margin_large</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="CourseLevelMain">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_size_med</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="CourseName">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_size_lg</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>
